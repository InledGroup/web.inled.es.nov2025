---
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SwiftInstall</title>
    <meta http-equiv="refresh" content="3;url=/apps/appinstall" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0a0a0a;
            color: #ffffff;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            margin: 0;
        }
        h1 {
            margin-bottom: 1rem;
        }
        p {
            color: #a0a0a0;
            margin-bottom: 2rem;
        }
        .loader {
            width: 48px;
            height: 48px;
            border: 5px solid #FFF;
            border-bottom-color: #5271ff;
            border-radius: 50%;
            display: inline-block;
            box-sizing: border-box;
            animation: rotation 1s linear infinite;
        }

        @keyframes rotation {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
        a {
            color: #5271ff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>SwiftInstall ahora es AppInstall</h1>
    <p>Te estamos redirigiendo a la nueva página...</p>
    <span class="loader"></span>
    <p style="margin-top: 2rem; font-size: 0.9rem;">Si no eres redirigido automáticamente, <a href="/apps/appinstall">haz clic aquí</a>.</p>

    <script>
        setTimeout(() => {
            window.location.href = "/apps/appinstall";
        }, 3000);
    </script>
</body>
</html>
