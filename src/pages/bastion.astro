---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';

// Bastion Browser Welcome Page with Translations
---

<Layout title="Bastion Browser - Navegación Ultra-Segura" description="Navegación ultra-segura mediante aislamiento remoto y endurecimiento de red.">
    <Header />
    <div class="welcome-container">
        <div class="dot-overlay"></div>
        <div class="welcome-content">
            <header class="hero-section">
                <img src="https://hosted.inled.es/bastion.png" class="welcome-hero-logo" alt="Bastion Logo" />
                <div class="logo-wrapper">
                    <h1 class="welcome-logo">Bastion</h1>
                    <span class="beta-badge">BETA</span>
                </div>
                <p class="welcome-subtitle" data-es="Navegación ultra-segura mediante aislamiento remoto y endurecimiento de red." data-en="Ultra-secure browsing via remote isolation and network hardening.">
                    Navegación ultra-segura mediante aislamiento remoto y endurecimiento de red.
                </p>

                <!-- Quick Install - CENTRAL ELEMENT -->
                <div class="install-container" onclick="copyInstallCommand()" title="Hacer clic para copiar / Click to copy">
                    <span class="install-command" id="cmd-text">git clone https://github.com/InledGroup/bastion.browser.git && cd bastion.browser && docker compose up -d --build</span>
                    <div class="copy-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
                    </div>
                </div>
            </header>

            <div class="bento-grid">
                <!-- Session Isolation -->
                <div class="bento-card card-large">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10"/></svg>
                    </div>
                    <div class="card-visual-glow"></div>
                    <h4 data-es="Aislamiento por Sesión (UUID)" data-en="Session Isolation (UUID)">Aislamiento por Sesión (UUID)</h4>
                    <p data-es="Cada conexión genera un entorno de ejecución único y totalmente estanco. Tus archivos y datos de navegación nunca se cruzan con otros usuarios." data-en="Each connection generates a unique and completely sealed execution environment. Your files and browsing data never cross with other users.">
                        Cada conexión genera un entorno de ejecución único y totalmente estanco. Tus archivos y datos de navegación nunca se cruzan con otros usuarios.
                    </p>
                </div>

                <!-- Restricted Access -->
                <div class="bento-card card-medium">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><polyline points="16 11 18 13 22 9"/></svg>
                    </div>
                    <h4 data-es="Acceso Restringido" data-en="Restricted Access">Acceso Restringido</h4>
                    <p data-es="Capa de autenticación mediante API Key obligatoria. Solo usuarios autorizados pueden establecer túneles de control hacia el bastión." data-en="Mandatory API Key authentication layer. Only authorized users can establish control tunnels to the bastion.">
                        Capa de autenticación mediante API Key obligatoria. Solo usuarios autorizados pueden establecer túneles de control hacia el bastión.
                    </p>
                </div>

                <!-- Anti-SSRF -->
                <div class="bento-card card-wide">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
                    </div>
                    <h4 data-es="Protección Anti-SSRF" data-en="Anti-SSRF Protection">Protección Anti-SSRF</h4>
                    <p data-es="Filtro dinámico de peticiones que bloquea el acceso a redes internas, metadatos de nube y servicios privados desde el navegador remoto." data-en="Dynamic request filtering that blocks access to internal networks, cloud metadata, and private services from the remote browser.">
                        Filtro dinámico de peticiones que bloquea el acceso a redes internas, metadatos de nube y servicios privados desde el navegador remoto.
                    </p>
                </div>

                <!-- Resources -->
                <div class="bento-card card-wide">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="8" x="2" y="2" rx="2" ry="2"/><rect width="20" height="8" x="2" y="14" rx="2" ry="2"/><line x1="6" y1="6" x2="6.01" y2="6"/><line x1="6" y1="18" x2="6.01" y2="18"/></svg>
                    </div>
                    <h4 data-es="Control de Recursos" data-en="Resource Control">Control de Recursos</h4>
                    <p data-es="Gestión activa de sesiones concurrentes y límites de memoria para garantizar la disponibilidad y prevenir ataques de denegación de servicio." data-en="Active management of concurrent sessions and memory limits to ensure availability and prevent denial of service attacks.">
                        Gestión activa de sesiones concurrentes y límites de memoria para garantizar la disponibilidad y prevenir ataques de denegación de servicio.
                    </p>
                </div>

                <!-- Encryption -->
                <div class="bento-card card-wide">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                    </div>
                    <h4 data-es="Cifrado de Extremo a Extremo" data-en="End-to-End Encryption">Cifrado de Extremo a Extremo</h4>
                    <p data-es="Todo el tráfico de control y el screencast viajan a través de túneles HTTPS/WSS cifrados con TLS de última generación." data-en="All control traffic and screencast travel through encrypted HTTPS/WSS tunnels with latest generation TLS.">
                        Todo el tráfico de control y el screencast viajan a través de túneles HTTPS/WSS cifrados con TLS de última generación.
                    </p>
                </div>

                <!-- Downloads -->
                <div class="bento-card card-wide">
                    <div class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m13 2-2 2.5h3L12 7"/><path d="M12 13v8"/><path d="m15 18-3 3-3-3"/><path d="M17 14.5a3 3 0 0 1 3 3 3 3 0 0 1-3 3h-1"/><path d="M7 14.5a3 3 0 0 0-3 3 3 3 0 0 0 3 3h1"/><path d="M12 2v2"/><path d="M12 8v2"/></svg>
                    </div>
                    <h4 data-es="Descargas Seguras" data-en="Secure Downloads">Descargas Seguras</h4>
                    <p data-es="Los archivos se capturan primero en el servidor en un área de staging aislada antes de ser transferidos de forma segura a tu equipo local." data-en="Files are first captured on the server in an isolated staging area before being securely transferred to your local machine.">
                        Los archivos se capturan primero en el servidor en un área de staging aislada antes de ser transferidos de forma segura a tu equipo local.
                    </p>
                </div>
            </div>

            <div class="status-bar">
                <div class="status-dot"></div>
                <span id="status-text" data-es="BASTIÓN SEGURO Y OPERATIVO" data-en="BASTION SECURE AND OPERATIONAL">BASTIÓN SEGURO Y OPERATIVO</span>
            </div>
        </div>
    </div>
</Layout>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap');

    :root {
        --bg-black: #000000;
        --bg-card: #09090b;
        --border-white: rgba(255, 255, 255, 0.08);
        --text-primary: #ffffff;
        --text-secondary: #a1a1aa;
        --accent-white: #ffffff;
        --font-main: 'Plus Jakarta Sans', sans-serif;
        --accent-green: #10b981;
    }

    .welcome-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        min-height: 100vh;
        background-color: var(--bg-black);
        color: var(--text-primary);
        font-family: var(--font-main);
        overflow-y: auto;
        overflow-x: hidden;
        position: relative;
        padding-top: 120px; /* Increased to avoid overlap with fixed header */
        padding-bottom: 100px;
    }

    /* Noise Texture Overlay */
    .welcome-container::before {
        content: '';
        position: fixed;
        inset: 0;
        z-index: 10;
        opacity: 0.05;
        pointer-events: none;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    }

    /* Abstract Dot Pattern */
    .welcome-container .dot-overlay {
        position: absolute;
        inset: 0;
        background-image: radial-gradient(circle at 1px 1px, rgba(255, 255, 255, 0.03) 1px, transparent 0);
        background-size: 24px 24px;
        pointer-events: none;
        z-index: 0;
    }

    /* Top Glow */
    .welcome-container::after {
        content: '';
        position: absolute;
        top: -100px;
        left: 50%;
        transform: translateX(-50%);
        width: 800px;
        height: 400px;
        background: radial-gradient(ellipse at center, rgba(255, 255, 255, 0.05) 0%, rgba(0, 0, 0, 0) 70%);
        pointer-events: none;
    }

    .welcome-content {
        max-width: 1100px;
        width: 100%;
        padding: 40px; /* Reduced top padding since container already has padding-top */
        z-index: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .hero-section {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        margin-bottom: 60px;
        animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        width: 100%;
    }

    .welcome-hero-logo {
        display: block;
        width: 120px;
        height: 120px;
        margin: 0 auto 24px auto;
        object-fit: contain;
        filter: drop-shadow(0 0 20px rgba(255, 255, 255, 0.1));
    }

    .welcome-logo {
        font-size: 8rem;
        font-weight: 800;
        margin: 0;
        letter-spacing: -0.06em;
        line-height: 0.9;
        text-transform: uppercase;
        background: linear-gradient(to bottom, #fff 30%, rgba(255, 255, 255, 0.2) 100%);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        position: relative;
    }

    .logo-wrapper {
        position: relative;
        display: block;
        margin-bottom: 24px;
    }

    .beta-badge {
        position: absolute;
        top: 0;
        right: -55px;
        font-size: 0.7rem;
        font-weight: 700;
        color: var(--accent-green);
        background: rgba(16, 185, 129, 0.1);
        border: 1px solid rgba(16, 185, 129, 0.2);
        padding: 2px 8px;
        border-radius: 4px;
        letter-spacing: 0.05em;
        text-transform: uppercase;
    }

    .welcome-subtitle {
        font-size: 1.5rem;
        color: var(--text-secondary);
        margin-top: 24px;
        font-weight: 400;
        letter-spacing: -0.02em;
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
        margin-bottom: 48px;
    }

    /* Installation Command - Central Focus */
    .install-container {
        margin-top: 20px;
        width: 100%;
        max-width: 800px;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid var(--border-white);
        border-radius: 24px;
        padding: 24px 32px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-family: monospace;
        font-size: 1.1rem;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
        backdrop-filter: blur(10px);
        margin-left: auto;
        margin-right: auto;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }

    .install-container:hover {
        background: rgba(255, 255, 255, 0.06);
        border-color: rgba(255, 255, 255, 0.2);
        transform: scale(1.02);
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4);
    }

    .install-command {
        color: var(--accent-green);
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        letter-spacing: -0.01em;
    }

    .copy-icon {
        color: var(--text-secondary);
        flex-shrink: 0;
        margin-left: 16px;
        transition: color 0.2s;
    }

    .install-container:hover .copy-icon {
        color: #fff;
    }

    .bento-grid {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        grid-auto-rows: minmax(320px, auto);
        gap: 16px;
        width: 100%;
        margin-top: 20px;
        animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s backwards;
    }

    .bento-card {
        background: var(--bg-card);
        border: 1px solid var(--border-white);
        border-radius: 28px;
        padding: 40px;
        position: relative;
        overflow: hidden;
        transition: transform 0.3s ease, border-color 0.3s ease, background 0.3s ease;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        text-align: left;
    }

    .bento-card:hover {
        border-color: rgba(255, 255, 255, 0.15);
        background: #121214;
        transform: translateY(-4px);
    }

    .card-large { grid-column: span 7; }
    .card-medium { grid-column: span 5; }
    .card-wide { grid-column: span 6; }
    .card-accent { grid-column: span 4; }
    .card-slim { grid-column: span 3; }

    .card-icon {
        width: 52px;
        height: 52px;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid var(--border-white);
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        transition: transform 0.3s ease;
        margin-bottom: 32px;
        flex-shrink: 0;
    }

    .bento-card:hover .card-icon {
        transform: scale(1.1) rotate(5deg);
    }

    .bento-card h4 {
        font-size: 1.5rem;
        font-weight: 600;
        margin: 0 0 12px 0;
        letter-spacing: -0.02em;
        color: #fff;
        line-height: 1.3;
    }

    .bento-card p {
        font-size: 1.05rem;
        color: var(--text-secondary);
        margin: 0;
        line-height: 1.6;
        max-width: 100%;
    }

    .card-visual-glow {
        position: absolute;
        top: -20%;
        right: -20%;
        width: 60%;
        height: 60%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.05) 0%, transparent 70%);
        pointer-events: none;
    }

    .status-bar {
        margin-top: 80px;
        display: flex;
        align-items: center;
        gap: 12px;
        background: rgba(255, 255, 255, 0.03);
        padding: 10px 20px;
        border-radius: 100px;
        border: 1px solid var(--border-white);
        font-size: 0.9rem;
        color: var(--text-secondary);
        letter-spacing: 0.05em;
        text-transform: uppercase;
        font-weight: 500;
    }

    .status-dot {
        width: 6px;
        height: 6px;
        background: #fff;
        border-radius: 50%;
        box-shadow: 0 0 8px #fff;
        animation: pulse-glow 2s infinite;
    }

    @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(40px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse-glow {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.3; transform: scale(0.8); }
    }

    @media (max-width: 1024px) {
        .bento-grid {
            grid-template-columns: 1fr;
            grid-auto-rows: auto;
        }
        .card-large, .card-medium, .card-wide, .card-accent, .card-slim {
            grid-column: span 1;
        }
        .welcome-logo { font-size: 4rem; }
        .install-container { flex-direction: column; gap: 10px; text-align: center; padding: 16px; font-size: 0.9rem; }
    }
</style>

<script is:inline>
    function copyInstallCommand() {
        const cmdText = document.getElementById('cmd-text').innerText;
        navigator.clipboard.writeText(cmdText).then(() => {
            const container = document.querySelector('.install-container');
            const originalBg = container.style.background;
            container.style.background = 'rgba(16, 185, 129, 0.2)';
            setTimeout(() => {
                container.style.background = originalBg;
            }, 200);
        });
    }

    document.addEventListener('DOMContentLoaded', () => {
        const setLanguage = (lang) => {
            const elements = document.querySelectorAll('[data-es]');
            elements.forEach(el => {
                const translation = el.getAttribute(`data-${lang}`);
                if (translation) {
                    el.innerText = translation;
                }
            });
        };

        // Listen for language changes from the Header component
        window.addEventListener('langChanged', (e) => {
            setLanguage(e.detail.lang);
        });

        // Initialize language from localStorage
        const savedLang = localStorage.getItem('preferred-lang') || 'es';
        setLanguage(savedLang);
    });
</script>
